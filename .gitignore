.vscode/
.DS_Store

# Build artifacts
/bin/

# Default data folder (--datafolder) and SQLite database files
/data/
*.db
*.db-shm
*.db-wal

# Local config copy (may contain auth tokens).
# The shipped demo lives at server/etc/pingmon.conf and stays tracked.
/pingmon.conf
/server/pingmon.conf
